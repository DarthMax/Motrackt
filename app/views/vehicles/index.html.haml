.row
  .span12
    - model_class = Vehicle
    .page-header
      %h1=t '.title', :default => model_class.model_name.human.pluralize

    =render :partial => "vehicle_list", :locals => {:vehicles => @vehicles}

    =button_to "New Vehicle", new_vehicle_path, :class => "btn btn-primary", :method => :get